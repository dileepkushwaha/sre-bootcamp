apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "student-api.fullname" . }}-test-connection"
  labels:
    {{- include "student-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "student-api.fullname" . }}:{{ .Values.api.service.port }}']  # Updated to match values.yaml
  restartPolicy: Never
